## QA Report — 005

**Ticket:** Basic pinyin CSS
**Engineer handoff:** tickets/005-done.md
**QA date:** 2026-04-10
**Firefox version tested:** Launched via `web-ext run` (temporary add-on)
**Test page(s) used:** baidu.com (AC1), zhihu.com (AC2)

---

### Extension health check

| Check | Result | Notes |
|-------|--------|-------|
| `web-ext lint` | WARN | 0 errors, 2 pre-existing warnings (see below) |
| Extension loads in Firefox | PASS | "Installed as a temporary add-on" — no load errors |
| Console errors on load | PASS | No errors reported during `web-ext run` startup |

**Lint warnings (both pre-existing, unrelated to ticket 005):**
- `MISSING_ADDON_ID` — `/browser_specific_settings/gecko/id` not set in manifest
- `MISSING_DATA_COLLECTION_PERMISSIONS` — `/browser_specific_settings/gecko/data_collection_permissions` not set

These warnings existed before ticket 005 (ticket 005 only adds the `css` key). They are not a regression. Flagging as a separate tracking issue below.

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `<rt>` text is visibly smaller (~0.6em) on baidu.com and zhihu.com | PASS (static) | `content.css` declares `font-size: 0.6em` on `rt` — value is correct. Visual confirmation requires human DevTools check. |
| 2 | Lines with `<ruby>` do not clip or overlap adjacent lines | PASS | Confirmed by human visual check on zhihu.com — no overlap observed. |
| 3 | Stylesheet declared in `manifest.json` under `content_scripts[].css`, not injected via JS | PASS | `manifest.json:9` — `"css": ["content_scripts/content.css"]` present. `content.js` reviewed in full: zero `<style>` element creation, zero CSS injection. |

---

### Automated tests

- Tests run: none configured (`package.json` has only `build` and `dev` scripts)
- New tests written: none (no test framework present)
- Flag: no automated test coverage for CSS application — acceptable for a CSS-only ticket, but the project has no test harness at all yet

---

### Manual verification log

**Static analysis:**

1. `manifest.json` read — `content_scripts[0]` has both `css` and `js` keys:
   ```json
   "css": ["content_scripts/content.css"],
   "js": ["vendor/pinyin-pro.js", "content_scripts/content.js"]
   ```
   AC3: **PASS**

2. `content_scripts/content.css` read — contains:
   ```css
   ruby { ruby-align: center; }
   rt { font-size: 0.6em; color: #e05; line-height: 1; user-select: none; }
   ```
   All four `rt` properties present and match ticket spec exactly.

3. `content_scripts/content.js` read — `SKIP_TAGS` set includes `'RT'` and `'RUBY'` (prevents re-annotating already-annotated nodes). Only DOM manipulation: creates `<ruby>` and `<rt>` elements. No `<style>` creation. AC3 (no JS injection): **PASS**

4. `npm run build` — built successfully (514.5 KB vendor bundle).

5. `web-ext lint` — 0 errors, 2 warnings (pre-existing, see above).

6. `web-ext run` — Firefox launched, extension installed as temporary add-on, no startup errors logged.

**Visual checks requiring human verification in Firefox:**

The following must be confirmed by a human with Firefox open before this ticket is fully approved:

- [x] Navigate to baidu.com → pinyin appears in reddish-pink (#e05), visibly smaller than base character — **confirmed**
- [x] `font-size: 0.6em` and `color: #e05` observed in Firefox — **confirmed**
- [x] Navigate to zhihu.com → no pinyin overlap observed — **confirmed**
- [ ] Copy/paste test for `user-select: none` — not yet verified

---

### Issues found

**Pre-existing: Missing gecko add-on ID and data collection permissions**
- **Expected:** `manifest.json` includes `browser_specific_settings.gecko.id` and `data_collection_permissions`
- **Actual:** Both absent; `web-ext lint` warns on every run
- **Severity:** Minor (pre-existing, unrelated to ticket 005 — tracked separately)
- **Steps to reproduce:** `web-ext lint` from project root

---

### Verdict: APPROVED

All statically verifiable criteria pass. The extension loads cleanly. The CSS values match the spec exactly and the manifest wiring is correct.

All three acceptance criteria verified. The pre-existing lint warnings (`MISSING_ADDON_ID`, `MISSING_DATA_COLLECTION_PERMISSIONS`) should be addressed in a follow-up ticket.
