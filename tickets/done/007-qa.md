## QA Report — 007

**Ticket:** Add browser_action and storage permission to manifest
**Engineer handoff:** tickets/007-done.md
**QA date:** 2026-04-10
**Firefox version tested:** Launched via `npx web-ext run` (system Firefox)
**Test page(s) used:** Static code inspection + web-ext run startup verification + human visual confirmation

---

### Extension health check

| Check | Result |
|-------|--------|
| `web-ext lint` | PASS |
| Extension loads in Firefox (`web-ext run`) | PASS |
| No console errors on load | PASS (web-ext reported clean install; no errors in startup output) |

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `npm run lint` exits with 0 errors and 0 warnings | PASS (with caveat) | No `npm run lint` script exists. `npx web-ext lint` was used instead. Output: `errors 0 / notices 0 / warnings 0`. The `url.parse()` DeprecationWarning is from Node.js/web-ext internals — not an extension issue. Engineer flagged this; a follow-up ticket should add `npm run lint` to `package.json`. |
| 2 | Toolbar button appears; clicking opens popup panel | PASS | `web-ext run` installed cleanly. Toolbar button confirmed visible in Firefox. Click confirmed to open popup panel showing "PinyinOverlay". |
| 3 | Pinyin annotations still appear on Chinese pages — no regression | PASS | Pinyin ruby annotations confirmed present on live Chinese pages. No regression. |

---

### Automated tests

- Tests run: `npx web-ext lint`
- New tests written: none needed (manifest + stub HTML/CSS; no logic to unit test)
- All passing: YES

---

### Manual verification log

**AC1 — lint**

```
npx web-ext lint
```

Full output:
```
(node:20171) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized...
(from web-ext tool internals — not extension code)

Validation Summary:
  errors    0
  notices   0
  warnings  0
```

Result: PASS.

**AC2 — toolbar button and popup**

```
npx web-ext run --no-input
```

Output:
```
Running web extension from /Users/carlin/dev/pinyin_plugin
Use --verbose or --devtools to see logging
Installed /Users/carlin/dev/pinyin_plugin as a temporary add-on
The extension will reload if any source file changes
```

Firefox opened without error. Extension installed cleanly. No startup errors in web-ext output.

Static verification of popup files:
- `popup/popup.html` — valid HTML5, `<meta charset="utf-8">`, `<link rel="stylesheet" href="popup.css">` (resolves correctly relative to `popup/popup.html`), body contains `<p>PinyinOverlay</p>`.
- `popup/popup.css` — `min-width: 200px; min-height: 80px; margin: 0; padding: 12px; font-family: system-ui, sans-serif`. Prevents 0×0 blank panel.

Visual confirmation: toolbar button visible, tooltip "PinyinOverlay", click opens popup panel — **CONFIRMED by human.**

**AC3 — no regression on Chinese pages**

Content script configuration in `manifest.json` is byte-for-byte identical to the pre-ticket state:
- `matches: ["*://*/*"]`
- `js: ["vendor/pinyin-pro.js", "content_scripts/content.js"]`
- `css: ["content_scripts/content.css"]`
- `run_at: "document_idle"`

No logic changes. The only additions to the manifest are `browser_action` and `permissions` — neither has any effect on content script injection behaviour.

Live browser verification: pinyin ruby annotations confirmed present on Chinese pages — **CONFIRMED by human.**

---

### Issues found

**Note (not a bug): `npm run lint` script missing**
- **Expected:** AC1 says `npm run lint` exits 0.
- **Actual:** `npm run lint` is not defined in `package.json`. The underlying command `npx web-ext lint` was used and passes.
- **Severity:** Minor — engineer already flagged this. Needs a follow-up ticket to wire `npm run lint` in `package.json`.

---

### Verdict: APPROVED

All three acceptance criteria pass. `web-ext lint` is clean (0 errors / 0 warnings / 0 notices). Extension installs without errors. Toolbar button visible in Firefox, popup opens on click, pinyin annotations confirmed present on live Chinese pages — no regression. One minor follow-up needed: add `npm run lint` script to `package.json` so the AC wording matches the available command.
