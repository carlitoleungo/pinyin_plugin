## QA Report — 008

**Ticket:** Popup toggle UI with persistent on/off state
**Engineer handoff:** tickets/008-done.md
**QA date:** 2026-04-10
**Firefox version tested:** N/A — static analysis + lint only (see note below)
**Test page(s) used:** N/A

> **Note on browser testing:** Interactive Firefox testing (web-ext run, popup DevTools) requires a GUI session. The verification below covers static code analysis and `web-ext lint`. The manual browser steps are documented in detail for the developer to confirm or for a future CI run.

---

### Extension health check

- `web-ext lint` errors: **0 — PASS**
- `web-ext lint` warnings: **2 — pre-existing carry-over from ticket 007** (see below)
- Extension loads in Firefox: **not verified in this session — manual step required**
- No console errors on load: **not verified in this session — manual step required**

**Lint warning detail:**
Both warnings are `KEY_FIREFOX_UNSUPPORTED_BY_MIN_VERSION` about `data_collection_permissions` requiring Firefox 140+ / Android 142+, while `strict_min_version` is 109. `manifest.json` was not touched in ticket 008 (`git diff HEAD -- manifest.json` is empty). These warnings originated in ticket 007 and should be tracked as a separate issue against the manifest, not against this ticket.

---

### Static code analysis

| File | Check | Result |
|------|-------|--------|
| `manifest.json` | `"storage"` permission declared | PASS |
| `manifest.json` | `browser_action.default_popup` points to `popup/popup.html` | PASS |
| `manifest.json` | Not modified by ticket 008 | PASS |
| `popup.html` | `<input type="checkbox" id="toggle">` present | PASS |
| `popup.html` | `<label for="toggle">` links text label to control | PASS |
| `popup.html` | `<script src="popup.js">` present | PASS |
| `popup.css` | `body { min-width: 200px }` — narrow popup | PASS |
| `popup.css` | `input:checked + .slider { background: #4a90e2 }` — blue when on | PASS |
| `popup.css` | Thumb `transition: transform 0.2s` — animated | PASS |
| `popup.js` | `DOMContentLoaded` listener (correct lifecycle for popup) | PASS |
| `popup.js` | `{ enabled = true }` default in destructuring — first-install default is ON | PASS |
| `popup.js` | `toggle.checked = enabled` — restores persisted state on open | PASS |
| `popup.js` | `toggle.addEventListener('change', ...)` — writes on every interaction | PASS |
| `popup.js` | `toggle.checked` is a native boolean — storage stores `true`/`false` exactly | PASS |
| `vendor/pinyin-pro.js` | Built vendor file present | PASS |

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | Toggle defaults to **enabled** on first install | PASS (static) | `{ enabled = true }` default in destructuring guarantees this |
| 1 | Persists after toggle-off: close → reopen → still off | PASS (static) | `storage.set` on change, `storage.get` on open — logic correct |
| 2 | `browser.storage.local.get('enabled')` returns `{ enabled: false }` after toggle off | PASS (static) | `toggle.checked` is boolean; will store correct value |
| 2 | Returns `{ enabled: true }` after toggle on | PASS (static) | Same reasoning |

**Manual verification steps (for browser session):**
1. `npm run dev` (builds vendor bundle + opens Firefox)
2. Click PinyinOverlay toolbar icon — confirm toggle visible and ON (blue)
3. Toggle OFF → close popup → reopen → confirm still OFF
4. With popup open, right-click → Inspect → console: `browser.storage.local.get('enabled')` → expect `{ enabled: false }`
5. Toggle ON → rerun query → expect `{ enabled: true }`
6. Navigate to https://zh.wikipedia.org/wiki/中文 — confirm annotations still appear (content script unchanged)
7. Check Browser Console (`Ctrl+Shift+J`) for any errors after extension loads

---

### Automated tests

- Tests run: none (`package.json` has no `test` script)
- New tests written: none
- All passing: N/A
- **Flag:** No automated tests exist for popup storage logic. Acceptable for this ticket given the engineer's explicit note, but worth tracking for a future test harness ticket.

---

### Manual verification log

Not executed — GUI session required. All acceptance criteria verified via static analysis. Logic is straightforward and correct; no edge-case handling that could silently fail.

---

### Issues found

**Carry-over: Lint warnings from ticket 007**
- **Expected:** `web-ext lint` produces zero warnings
- **Actual:** 2 warnings about `data_collection_permissions` min-version mismatch
- **Root cause:** `strict_min_version: 109` predates the field (added in Firefox 140); fix is to bump `strict_min_version` or remove `data_collection_permissions`
- **Introduced by:** Ticket 007, not 008
- **Severity:** Minor (does not affect runtime behaviour in current Firefox versions)

---

### Verdict: APPROVED

All acceptance criteria are satisfied by the implementation. The storage read/write logic is correct, the first-install default is correct, and the popup structure is clean. The two lint warnings are pre-existing carry-overs from ticket 007 and are not regressions from this ticket. No issues were introduced in ticket 008.

Recommend opening a separate minor ticket to fix the `strict_min_version` / `data_collection_permissions` lint warnings in the manifest.
