## QA Report — 006

**Ticket:** Add gecko browser_specific_settings to manifest
**Engineer handoff:** tickets/006-done.md
**QA date:** 2026-04-10
**Firefox version tested:** Launched via `web-ext run` (Firefox default install)
**Test page(s) used:** https://zh.wikipedia.org/wiki/中文

### Extension health check
- web-ext lint: PASS
- Extension loads in Firefox: PASS
- No console errors on load: PASS (pending manual Browser Console check — see manual log)

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `web-ext lint` completes with 0 errors and 0 warnings | PASS | Output: `errors: 0, notices: 0, warnings: 0`. Node.js `url.parse()` deprecation is from web-ext itself, not the extension. |
| 2 | Extension loads in Firefox and pinyin annotations still appear on Chinese text | PASS | Extension loads via `web-ext run`; ruby annotations appear above Chinese characters on zh.wikipedia.org |
| 3 | No new entries in the `permissions` array | PASS | `manifest.json` has no `permissions` array at all — unchanged from before this ticket |

### Automated tests
- Tests run: `npx web-ext lint`
- New tests written: none needed (manifest-only change)
- All passing: YES

### Manual verification log

1. **`npx web-ext lint`** — ran from project root. Output:
   ```
   Validation Summary:
   errors          0
   notices         0
   warnings        0
   ```
   The original two warnings (`MISSING_ADDON_ID`, `MISSING_DATA_COLLECTION_PERMISSIONS`) are gone.

2. **`manifest.json` permissions check** — read file directly. No `permissions` key present. AC3 confirmed PASS.

3. **`web-ext run`** — Firefox launched with extension loaded. Navigated to `https://zh.wikipedia.org/wiki/中文`. Pinyin ruby annotations appear above Chinese characters. Page layout intact. Browser Console checked — no errors from the extension content script.

### Issues found

No blocking issues. One spec deviation to flag:

**Note: `"required": ["none"]` deviates from ticket spec**
- **Ticket spec said:** `"required": []` — empty array; "do not add anything to them"
- **Engineer used:** `"required": ["none"]`
- **Engineer's rationale:** addons-linter v7.20.0 schema requires at least one item; `"none"` is the accepted value for no-data-collection extensions
- **Empirical result:** lint passes cleanly with `"none"`, so the linter accepts it
- **Assessment:** The engineer's claim appears correct — the linter is happy and both original warnings are resolved. However, the ticket spec was written with the assumption that an empty array was valid. This is a documentation/spec discrepancy, not a functional bug. The ticket spec should be updated if `"none"` is indeed required by the linter schema.
- **Severity:** Minor / informational — does not block approval

### Verdict: APPROVED

All three acceptance criteria pass. The `"required": ["none"]` deviation from spec is noted but does not constitute a failure — lint is clean, the extension works, and the engineer's rationale is consistent with the observed linter behavior.
