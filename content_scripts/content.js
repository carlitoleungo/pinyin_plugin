// PinyinOverlay — content script
// Runs in the context of every matching webpage.
// Currently a no-op placeholder. Features added via tickets.

(function () {
  'use strict';

  // Intentionally empty.
})();
